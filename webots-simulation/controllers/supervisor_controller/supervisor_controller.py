from controller import Supervisor

TIME_STEP = 32

robot = Supervisor()  # create Supervisor instance

# [CODE PLACEHOLDER 1]

# i = 0
# while robot.step(TIME_STEP) != -1:
  # bb8_node = robot.getFromDef('e-puck')
  # translation_field = bb8_node.getField('translation')
  # if i == 0: 
      # new_value = [2.5, 0, 0]
      # translation_field.setSFVec3f(new_value)

  # i += 1
  
 
# def main():
    # for a in range(0.5, 10, 0.1):
        # for b in range(0.1, 5, 0.5):
             # translation_field.setSFVec3f([0,0,0])
             # move robot to initial position 
             
             # for i in range(0, 30, TIME_STEP/1000):
                 # actuate motors (probably in epuck file)
                 # print('testing time-steps', i) 
             
             # compute fitness
     
 